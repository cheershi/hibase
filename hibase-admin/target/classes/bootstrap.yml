server:
  port: ${SERVER_PORT:8010}
spring:
  application:
    name: hibase-admin
  cloud:
    nacos:
      discovery:
        server-addr: ${REGISTER_HOST:47.101.138.250}:${REGISTER_PORT:8848}
      config:
        server-addr: ${REGISTER_HOST:47.101.138.250}:${REGISTER_PORT:8848}
        file-extension: yml
